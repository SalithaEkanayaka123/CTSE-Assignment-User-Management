apiVersion: v1
kind: Service
metadata:
  name: CTSE-Assignment-User-Management
spec:
  selector:
    app: CTSE-Assignment-User-Management
  type: LoadBalancer
  ports:
    - port: 8080
      targetPort: 8080